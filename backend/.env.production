# Mirai HelpDesk Management System - Production Environment
# ============================================================
# 本番環境設定ファイル
# ポート番号: 8443 (API), 443 (WebUI) - 変更禁止

# Environment
NODE_ENV=production
ENVIRONMENT=production
APP_NAME=Mirai HelpDesk Management System
APP_VERSION=2.0.0

# Server Configuration
HOST=192.168.0.187
BACKEND_PORT=8443
FRONTEND_PORT=443
USE_HTTPS=true

# SSL Certificates (Self-signed)
SSL_CERT_PATH=./certificates/server.crt
SSL_KEY_PATH=./certificates/server.key

# API
API_PREFIX=/api
DEBUG=false

# Database
# PRODUCTION MUST USE PostgreSQL! SQLite is not recommended for production.
# DATABASE_URL=sqlite+aiosqlite:///./data/production/helpdesk.db  # DO NOT USE IN PRODUCTION

# PostgreSQL Configuration (REQUIRED for production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mirai_helpdesk
DB_USER=mirai_user
DB_PASSWORD=CHANGE-THIS-TO-SECURE-PASSWORD
DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Database Connection Pool (PostgreSQL)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=false

# Authentication - CHANGE THIS IN PRODUCTION!
SECRET_KEY=CHANGE-THIS-TO-A-STRONG-SECRET-KEY-IN-PRODUCTION
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=1

# CORS
CORS_ORIGINS=["https://192.168.0.187"]

# Logging
LOG_LEVEL=warning
LOG_FILE=./logs/production/app.log

# File Upload
UPLOAD_DIR=./data/production/uploads
MAX_UPLOAD_SIZE_MB=10

# Microsoft Graph API (Non-interactive authentication)
# 実際の値は環境変数または .env ファイル（Git無視）で設定してください
MS_TENANT_ID=your-tenant-id-here
MS_CLIENT_ID=your-client-id-here
MS_CLIENT_SECRET=your-client-secret-here
MS_AUTHORITY=https://login.microsoftonline.com/{your-tenant-id}
MS_GRAPH_ENDPOINT=https://graph.microsoft.com/v1.0
MS_SCOPES=https://graph.microsoft.com/.default

# SLA Defaults (hours)
SLA_P1_RESPONSE=1
SLA_P1_RESOLUTION=4
SLA_P2_RESPONSE=4
SLA_P2_RESOLUTION=24
SLA_P3_RESPONSE=8
SLA_P3_RESOLUTION=72
SLA_P4_RESPONSE=24
SLA_P4_RESOLUTION=120

# Production Options
INCLUDE_SAMPLE_DATA=false
ENABLE_API_DOCS=false
