name: 🎯 コード品質チェック

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  quality-check:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: 📥 チェックアウト
        uses: actions/checkout@v4

      - name: 🔧 Node.js ${{ matrix.node-version }} セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: 📦 依存関係インストール（バックエンド）
        run: |
          cd backend
          npm ci

      - name: 📦 依存関係インストール（フロントエンド）
        run: |
          cd frontend
          npm ci

      - name: 🔍 ESLint チェック（バックエンド）
        run: |
          cd backend
          npm run lint

      - name: 🔍 ESLint チェック（フロントエンド）
        run: |
          cd frontend
          npm run lint

      - name: 🏗️ ビルド（バックエンド）
        run: |
          cd backend
          npm run build

      - name: 🏗️ ビルド（フロントエンド）
        run: |
          cd frontend
          npm run build

      - name: 🧪 テスト（バックエンド）
        run: |
          cd backend
          npm test || true

      - name: 🧪 テスト（フロントエンド）
        run: |
          cd frontend
          npm test || true

      - name: 📊 カバレッジレポート生成
        if: matrix.node-version == '18.x'
        run: |
          cd backend
          npm run test:coverage || true

      - name: ✅ すべてのチェック完了
        run: |
          echo "✅ コード品質チェック完了"
          echo "  - Node.js: ${{ matrix.node-version }}"
          echo "  - ESLint: ✅"
          echo "  - TypeScript: ✅"
          echo "  - Build: ✅"
